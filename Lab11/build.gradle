application.mainClassName = "com.demkom58.lab11.Main"

jar {
    manifest { attributes('Main-Class': application.mainClassName) }
    from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
}