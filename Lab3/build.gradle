application {
    mainClassName = "com.demkom58.lab3.Main"
}

dependencies {
}

jar {
    manifest {
        attributes('Main-Class': application.mainClassName)
    }
    from {
        configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
    }
}